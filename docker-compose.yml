services:
  backend:
    build: ./Backend
    volumes:
      - ./Backend:/app
    environment:
      - PORT=8000

  frontend:
    build: ./Frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
