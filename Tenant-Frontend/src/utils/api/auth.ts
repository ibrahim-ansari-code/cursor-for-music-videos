// Authentication API Functions for tenant portal

// Re-export authentication functions from core
// Note: login is handled through Supabase Auth, not the backend API
export { getCurrentUser } from './core';

