import React from 'react';
import SettingsContent from '@/components/settings/SettingsContent';

/**
 * Settings Page
 * Tenant account settings and preferences
 */
const Settings: React.FC = () => {
  return <SettingsContent />;
};

export default Settings;
