[pytest]
# Test discovery
testpaths = Backend/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Async support - EXPLICIT configuration to fix deprecation warning
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

# Test markers
markers = 
    asyncio: marks tests as async tests (pytest-asyncio)
    auth: marks tests that require authentication
    slow: marks tests as slow  
    integration: marks tests as integration tests
    unit: marks tests as unit tests
    api: marks tests as API tests

# Logging configuration
log_cli = true
log_cli_level = INFO
log_cli_format = %(asctime)s [%(levelname)8s] %(message)s
log_cli_date_format = %Y-%m-%d %H:%M:%S

# Output configuration
# Sequential execution (parallel execution disabled)
addopts = -v --tb=short --strict-markers --durations=10 --maxfail=100 -p pytest_asyncio
# -n auto: Parallel execution with optimal worker count (based on CPU cores)
# --durations=10: Show 10 slowest tests for optimization
# --maxfail=100: Stop after 100 failures to avoid wasting time on broken test runs
# Distribution modes can be set with --dist flag:
# --dist each: Send each test to any available worker (default)
# --dist loadscope: Send tests grouped by module to same worker
# --dist loadfile: Send all tests in a file to same worker
# --dist loadgroup: Send tests marked with same group to same worker

# Minimum Python version
minversion = 3.11

# WARNING CAPTURE CONFIGURATION - Show ALL warnings with full details
filterwarnings =
    # Show all warnings with full traceback (default behavior)
    default
    # Specific warning categories to capture with full details
    default::DeprecationWarning
    default::RuntimeWarning  
    default::UserWarning
    default::PendingDeprecationWarning
    default::ResourceWarning
    # To turn warnings into errors instead (uncomment next line):
    # error 